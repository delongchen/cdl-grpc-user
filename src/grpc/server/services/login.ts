import {GrpcServiceImplFn} from "../../../types/grpc";
import {LoginRequest, LoginResponse} from "cdl-grpc";

export const loginHandler: GrpcServiceImplFn<LoginRequest, LoginResponse> = (call, callback) => {
  
}
